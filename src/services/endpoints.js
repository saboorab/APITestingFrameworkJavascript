//Defining the URL configurable such as Base_URI so that test can be executed in multiple environments only changing the BASE_URI
export const BASE_URI = "https://api-demo.airwallex.com";
//Defining the AUTH_LOGIN endpoints
export const AUTH_LOGIN = '/api/v1/authentication/login';
//Defining the create beneficiary endpoints
export const CREATE_BENEFICIARIES = '/api/v1/beneficiaries/create';